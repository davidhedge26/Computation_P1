Make project plan here:
  => build DFA.java
  => build DFAState.java  

##Initialization Stage - Progress towards the goals of the initial tests [Build DFA].  
1. 5 tuple obj's Initialized
2. interfaces implemented, and abstract classes extended
3. Pass values into objects through interface helper methods
4. Keep working on the initial helper methods to construct DFA
